# Раздел Go

## Исходные материалы

- `Go_задания ч.1.docx` — основной файл с заданиями;
- `tasks1/` — оформленная и выполненная часть 1.

## Структура выполненной работы

В каталоге `tasks1/` находятся:

- `report.md` — текстовый отчёт;
- `otchet-compose.yml` — конфигурация генерации DOCX;
- `build/Go-tasks1-report.docx` — итоговый отчёт;
- подкаталоги `01-...` - `10-...` с исходными примерами.

## Метаданные отчётов

- студент: `Купленик С. С.`;
- группа: `УВП-171`;
- преподаватель: `Старший преподаватель кафедры ЦТУТП Заманов Е. А.`.
