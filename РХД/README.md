# Раздел РХД

## Исходное задание

- `РХД. PG.docx` — основной файл с заданиями по PostgreSQL.

## Выполненные работы

Оформлены отдельные лабораторные работы:

- `lab1/` — создание структуры базы данных;
- `lab2/` — загрузка CSV и ANALYZE;
- `lab3/` — логическое резервное копирование и восстановление;
- `lab4/` — физическая репликация PostgreSQL.

В каждом каталоге `labN/` находятся:

- `report.md` — текстовый отчёт;
- `otchet-compose.yml` — конфигурация генерации DOCX;
- `build/labN-report.docx` — итоговый отчёт.

## Метаданные отчётов

- студент: `Купленик С. С.`;
- группа: `УВП-171`;
- преподаватель: `Старший преподаватель кафедры ЦТУТП Заманов Е. А.`.
