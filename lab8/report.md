# Отчёт

## Данные по работе

- Студент: `Купленик С. С.`
- Группа: `УВП-171`
- Преподаватель: `Старший преподаватель кафедры ЦТУТП Заманов Е. А.`

## РџРѕРґСЂРѕР±РЅС‹Рµ РѕС‚РІРµС‚С‹ РєРѕРјР°РЅРґ

РљРѕРјР°РЅРґР°:

```powershell
curl -s node1.local:8080
```

РћС‚РІРµС‚ РїРѕСЃР»Рµ override:

```text
### overrided hello from node1.local !
```

РљРѕРјР°РЅРґР°:

```powershell
curl -s node1.local
```

Р¤РёРЅР°Р»СЊРЅС‹Р№ РѕС‚РІРµС‚:

```text
hello from node1.local
```





